# TODO define types and classes that correspond to AnnotatedDocV2 in `influx_core/src/lib.rs`. 
# The class should expose .to_dict() that returns the representation which can then be serialized to JSON and decoded in Rust.
